types = {
	type[terrain] = {
		path = "game/map"
		path_file = "terrain.txt"
		skip_root_key = categories
		localisation = {
			name = "$"
		}
	}
}

terrain = {
    ## cardinality = 0..inf
    alias_name[modifier] = alias_match_left[modifier]
}

# for now skip rest of file content
